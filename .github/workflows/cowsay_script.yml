name: Cowsay Script

on: [push]

jobs:
    cowsay:
      runs-on: ubuntu-22.04

      steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Add Permission
        run: sudo chmod u+x ./cowsay_script.sh
    
      - name: install cowsay 
        run: ./cowsay_script.sh